# Core
streamlit>=1.28.0,<1.30.0
python-dotenv>=1.0.0

# Data processing (use older compatible versions)
pandas>=1.5.0,<2.1.0
numpy>=1.23.0,<1.25.0

# Visualization
matplotlib>=3.7.0,<3.8.0
seaborn>=0.12.0,<0.13.0
plotly>=5.17.0,<5.18.0

# Database (use psycopg2-binary with specific version)
psycopg2-binary>=2.9.0,<2.10.0

# AI/ML (optional - remove if not using)
openai>=1.0.0,<1.30.0

# Remove google-generativeai for now to avoid conflicts
# google-generativeai>=0.3.0

# Utilities
rich>=13.0.0
