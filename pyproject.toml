[project]
name = "ai-db-query-app"
version = "1.0.0"
requires-python = ">=3.11,<3.12"

[tool.streamlit]
python_version = "3.11"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
